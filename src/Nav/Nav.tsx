import NavButton from "../NavButton/NavButton"
import "./Nav.scss"

const Nav = () => {
  return (
    <nav className="nav">
        <NavButton/>
        <NavButton/>
        
    </nav>
  )
}

export default Nav